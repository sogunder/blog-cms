export enum UserRole {
  Admin = 'admin',
  Editor = 'editor',
  Reader = 'reader',
}

export enum PostStatus {
  Draft = 'draft',
  Pending = 'pending',
  Published = 'published',
}

export enum CommentStatus {
  Pending = 'pending',
  Approved = 'approved',
  Spam = 'spam',
}
